import React from 'react'

const Container = ({children}) => {
  return <div className='w-full max-w-7xl mx-auto px-4'>{children}</div>;
  
}
//Container basically use in all company and the use of container is store some functionality of css and we can use this container anywhere.
//children is a name not more than name.
export default Container